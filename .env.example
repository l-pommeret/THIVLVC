# OpenRouter (any model: Gemini, Claude, Llama, Qwen, etc.)
OPENROUTER_API_KEY=your_openrouter_key_here

# For local models (Ollama/vLLM), no API key needed — just use:
#   --provider local --model qwen3:72b
